<!DOCTYPE html>
<html>
  <head>
    <title>Skills</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <a href="/">Home</a>
    <ul>
      <% skills.forEach(function(item) { %>
        <li>
          <a href="/skills/<%= item.id %>"><%= item.name %></a>
        </li>
      <% }) %>
    </ul>
  </body>
</html>
